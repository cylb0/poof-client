/**
 * Interface that represents an object storing data related to api calls
 */
export interface ApiConfig {
    /**
     * The URL of the api
     * @example `http://localhost:3333`
     */
    apiUrl: string
}